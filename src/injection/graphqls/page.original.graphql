type Page {
    # form data for page
    form: Json,
    # schema data for page
    schema: Json,
}

type Query {
    # get a page by identification
    getPage(identification: String): Page,
    # get all pages
    getPages: [Page],
}
