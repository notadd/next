type Query {
    getUsers: [User],
    getUserById(id: Int): User,
    getUserByUsername(username: String): User,
}

type Mutation {
    createUser(name: String): User
}

type User {
    username: String,
    email: String,
    password: String,
}
