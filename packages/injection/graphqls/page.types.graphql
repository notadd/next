type ConfigurationPage {
    # form data for page
    form: Json,
    # schema data for page
    schema: Json,
}

type Query {
    # get a page by identification
    getPage(identification: String): ConfigurationPage,
    # get all pages
    getPages: [ConfigurationPage],
}
